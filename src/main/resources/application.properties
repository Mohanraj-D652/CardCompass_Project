spring.application.name=cardcompass


# ?? Server ??????????????????????????????????????????????????????????????
server.port=9090
server.port=${PORT:9090}

# ?? JSP View Resolver ????????????????????????????????????????????????????


spring.mvc.view.prefix=/WEB-INF/views/
spring.mvc.view.suffix=.jsp

# ?? MySQL ? exact values from your DBConnection.java ????????????????????
# Local:  uses localhost/root/mysql
# Render: override with environment variables DB_HOST, DB_USER, DB_PASSWORD
spring.datasource.url=jdbc:mysql://${DB_HOST:centerbeam.proxy.rlwy.net}:${DB_PORT:23871}/${DB_NAME:credit_card_spending_db}?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=${DB_USER:root}
spring.datasource.password=${DB_PASSWORD:ZhNUJTuBcZgcMRbzdmPkXZYSZXWPKIORl}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ?? JPA ??????????????????????????????????????????????????????????????????
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.open-in-view=false

# ?? Email ? exact values from your MonthlyReportServlet.java ????????????
# Local:  uses your Gmail directly
# Render: override with MAIL_USER and MAIL_PASS environment variables
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USER:arjunmohanraj143@gmail.com}
spring.mail.password=${MAIL_PASS:cnjiucsmvafkikhq}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# ?? File upload ??????????????????????????????????????????????????????????
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# ?? Session ??????????????????????????????????????????????????????????????
server.servlet.session.timeout=30m
